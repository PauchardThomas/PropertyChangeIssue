<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Basket.MainPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackLayout>
        <Button Command="{Binding AddCommand}" Text="New" />
        <Label Text="Hello !" />
        <Label Text="{Binding Basket.Total, Mode=TwoWay}" />
        <StackLayout BindableLayout.ItemsSource="{Binding Basket.Products}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Entry Text="{Binding Value, Mode=TwoWay}" />
                    </StackLayout>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </StackLayout>

</ContentPage>
